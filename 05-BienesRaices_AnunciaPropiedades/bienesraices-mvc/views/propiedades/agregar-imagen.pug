extends ../layout/admin

block styles 

block contenido
    .py-10
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold= pagina

        div(class="bg-white shadow px-4 py-8 rounded mx-auto max-w-4xl my-10 md:px-10")
            form.space-y-8(method="POST" action="/propiedades/crear")
                //- Protección CSRF
                input(type="hidden", name="_csrf" value= csrfToken)

                input(type="submit" value="Publicar Propiedad" class="w-full py-3 bg-indigo-600 hover:bg-indigo-700 roundend text-white font-bold uppercase cursor-pointer")

block scrips
