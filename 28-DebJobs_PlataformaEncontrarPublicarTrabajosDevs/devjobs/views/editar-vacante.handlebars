<main class="contenedor">
    <form action="/vacantes/editar/{{vacante.url}}" method="POST" class="default-form">
        <h3>Información General</h3>
        <div class="campo">
            <label for="titulo">Titulo</label>
            <input type="text" name="titulo" id="titulo" value="{{vacante.titulo}}" placeholder="Ej: React Developer" required>
        </div>
        <div class="campo">
            <label for="empresa">Empresa</label>
            <input type="text" name="empresa" id="empresa" value="{{vacante.empresa}}" placeholder="Empresa Contrata" required>
        </div>
        <div class="campo">
            <label for="ubicacion">Ubicación</label>
            <input type="text" name="ubicacion" id="ubicacion" value="{{vacante.ubicacion}}" placeholder="Ej: Bolivia o Remoto" required>
        </div>
        <div class="campo">
            <label for="salario">Salario (USD)</label>
            <input type="text" name="salario" id="salario" value="{{vacante.salario}}" placeholder="Ej: 500 USD" required>
        </div>
        <div class="campo">
            <label for="contrato">Contrato</label>
            <select name="contrato" id="contrato">
                {{#tipoContrato vacante.contrato}}
                    <option value="" disabled selected>-- Selecciona --</option>
                    <option value="Freelance">Freelance</option>
                    <option value="Tiempo Conpleto">Tiempo Conpleto</option>
                    <option value="Medio Tiempo">Medio Tiempo</option>
                    <option value="Por Proyecto">Por Proyecto</option>
                {{/tipoContrato}}
            </select>
        </div>

                <h3>Descripción del Puesto</h3>
        <div class="campo descripcion">
            <label for="descripcion">Descripción</label>
            {{!-- Insertar el texto del trix editor en este input --}}
            <input type="hidden" id="x" name="descripcion" value="{{{vacante.descripcion}}}">       {{!-- {{{}}} para imprimir HTML --}}
            <trix-editor input="x"></trix-editor>
        </div>

        <h3>Conocimientos</h3>
        <ul class="lista-conocimientos">
            {{#seleccionarSkills vacante.skills}}            {{!-- x: puede ser cual caracter o texto (Resuelve el error de 'Cannot read properties of undefined (reading "  fn")') --}}
                {{!-- <li>Hola amigos de Udemy</li> --}}
            {{/seleccionarSkills}}
        </ul>

        <div class="campo centrar-horizontal">
            {{!-- Hidden inputs --}}
            <input type="hidden" name="skills" id="skills">

            <input type="submit" value="Guardar Cambios" class="btn btn-azul">
        </div>
    </form>
</main>